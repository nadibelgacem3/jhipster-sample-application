version: '2'
services:
  mooingateway-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/mooingateway/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=mooingateway
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
