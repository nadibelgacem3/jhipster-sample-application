version: '2'
services:
  mooincashier-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/mooincashier/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=mooincashier
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
